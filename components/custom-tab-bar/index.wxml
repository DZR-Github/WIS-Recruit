<view class="tab-bar">
  <view wx:for="{{list}}" wx:key="index" class="tab-bar-item" data-index="{{index}}" bindtap="switchTab">
    <image class="item-image" src="{{selected === index ? item.selectedIconPath : item.iconPath}}"></image>
  </view>
</view>